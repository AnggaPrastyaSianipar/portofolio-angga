---
title: "Sertifikat Pelatihan Digital Talent Scholarship: Pengantar Mindset Digital 1"
date: 2025-11-27

event: "Pusat Pengembangan Literasi Digital - Kementerian Komunikasi dan Digital"
event_url: "https://digitalent.kominfo.go.id"

location: "Pusat Pengembangan Literasi Digital, KOMDIGI"
address:
  country: "Indonesia"

summary: "Sertifikat Resmi Pelatihan Micro Skill dari KOMDIGI yang memvalidasi pemahaman fundamental transformasi pola pikir digital untuk masa depan."

# Waktu pelaksanaan mengikuti tahun pelatihan Anda (27 November 2025)
event_start: 2025-11-27
event_end: 2025-11-27
all_day: true

authors:
  - me

tags:
  - Certification
  - Digital Mindset
  - Literasi Digital
  - KOMDIGI
  - Digital Talent Scholarship

featured: true

image:
  caption: "Sertifikat Pelatihan KOMDIGI Mindset Digital (Klik gambar untuk Mode Theater HD)"
  focal_point: "Center"
---

## 📜 Deskripsi Pelatihan Kompetensi

Sertifikat dengan nomor **2299815850-19090/MS/BLSDM.Komdigi/2025** ini diterbitkan oleh **Kementerian Komunikasi dan Digital (KOMDIGI)** melalui **Pusat Pengembangan Literasi Digital**. Kredensial ini merupakan pengakuan formal atas penyelesaian pelatihan *Micro Skill* program **Digital Talent Scholarship 2025** dengan topik **Pengantar Mindset Digital 1: Mengubah Masa Depan Anda Dengan Pola Pikir Digital**.

---

### 🏛️ Materi & Kompetensi yang Divalidasi

Pelatihan intensif sepanjang **2 Jam Pelatihan (JP)** ini memvalidasi pemahaman dasar mengenai pentingnya adaptasi pola pikir di era transformasi digital, dengan rincian struktur materi sebagai berikut:

| Materi Pelatihan | Jumlah JP |
| :--- | :---: |
| Pengantar Mindset Digital | 0.3 |
| Apa itu Mindset Digital & Kenapa Penting? | 0.4 |
| Elemen Kunci Mindset Digital | 0.6 |
| Bagaimana Membangun Mindset Digital | 0.7 |
| **Total Durasi** | **2 Jam** |

* **Konsep Pola Pikir Digital**: Memahami definisi, urgensi, serta pergeseran paradigma dari pola pikir konvensional menuju pola pikir digital yang adaptif terhadap perkembangan teknologi.
* **Elemen Kunci & Implementasi**: Menguasai komponen-komponen utama penyusun *digital mindset* serta strategi praktis untuk membangun dan mengintegrasikannya dalam kehidupan profesional guna menghadapi perubahan masa depan.

> [!SUCCESS] Otentisitas Kredensial
> Sertifikat ini ditetapkan di Jakarta pada **27 November 2025** dan ditandatangani secara elektronik oleh **Bonifasius W. Pudjianto** selaku Kepala Badan Pengembangan SDM KOMDIGI, menggunakan sertifikat elektronik resmi yang diterbitkan oleh Balai Sertifikasi Elektronik (BSrE), Badan Siber dan Sandi Negara (BSSN).


<div id="custom-light-box" style="display: none; position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(5, 5, 5, 0.98); z-index: 999999; justify-content: center; align-items: flex-start; padding: 50px 20px; opacity: 0; transition: opacity 0.2s ease-in-out; overflow-y: auto;">
  <span id="close-light-box" style="position: fixed; top: 20px; right: 30px; color: #fff; font-size: 50px; font-weight: bold; cursor: pointer; user-select: none; z-index: 1000001; background: rgba(0,0,0,0.6); width: 60px; height: 60px; display: flex; justify-content: center; align-items: center; border-radius: 50%; line-height: 50px;">&times;</span>
  
  <span id="prev-light-box" style="position: fixed; top: 50%; left: 30px; color: #fff; font-size: 40px; font-weight: bold; cursor: pointer; user-select: none; z-index: 1000001; background: rgba(0,0,0,0.6); width: 50px; height: 50px; display: flex; justify-content: center; align-items: center; border-radius: 50%; transform: translateY(-50%);">&#10094;</span>
  <span id="next-light-box" style="position: fixed; top: 50%; right: 30px; color: #fff; font-size: 40px; font-weight: bold; cursor: pointer; user-select: none; z-index: 1000001; background: rgba(0,0,0,0.6); width: 50px; height: 50px; display: flex; justify-content: center; align-items: center; border-radius: 50%; transform: translateY(-50%);">&#10095;</span>

  <img id="light-box-img" src="" alt="Sertifikat Ultra HD" style="display: block; width: 100%; max-width: 1400px; height: auto; border-radius: 6px; box-shadow: 0 0 40px rgba(255,255,255,0.3); margin: 0 auto; cursor: zoom-out; image-rendering: -webkit-optimize-contrast; image-rendering: crisp-edges; transform: translateZ(0); backface-visibility: hidden;">
</div>

<script>
  document.addEventListener("DOMContentLoaded", function() {
    // 1. Definisikan array file gambar (halaman depan dan belakang)
    const imagesList = ["featured.jpg", "featured1.jpg"];
    let currentIdx = 0;

    // 2. Cari target komponen gambar utama bawaan dari tema website Anda
    const themeImgTarget = document.querySelector('.article-header img, .featured-image img, img[class*="featured"]');
    
    if (themeImgTarget) {
      const themeContainer = themeImgTarget.parentElement;
      
      // Mengubah struktur gambar tunggal statis menjadi sistem Slider Kustom interaktif
      const sliderHTML = `
        <div class="cert-slider-container" style="position: relative; width: 100%; max-width: 800px; margin: 0 auto 20px auto; overflow: hidden; border-radius: 8px; box-shadow: 0 4px 20px rgba(0,0,0,0.3);">
          <div class="cert-slider-slides" style="display: flex; transition: transform 0.4s ease-in-out;">
            <img class="cert-slide-img" src="featured.jpg" style="width: 100%; flex-shrink: 0; cursor: zoom-in;" title="Klik untuk Mode Theater HD">
            <img class="cert-slide-img" src="featured1.jpg" style="width: 100%; flex-shrink: 0; cursor: zoom-in;" title="Klik untuk Mode Theater HD">
          </div>
          <button id="slider-prev" style="position: absolute; top: 50%; left: 15px; transform: translateY(-50%); background: rgba(0,0,0,0.6); color: white; border: none; width: 45px; height: 45px; border-radius: 50%; font-size: 24px; cursor: pointer; display: flex; align-items: center; justify-content: center; z-index: 10; user-select: none;">&#10094;</button>
          <button id="slider-next" style="position: absolute; top: 50%; right: 15px; transform: translateY(-50%); background: rgba(0,0,0,0.6); color: white; border: none; width: 45px; height: 45px; border-radius: 50%; font-size: 24px; cursor: pointer; display: flex; align-items: center; justify-content: center; z-index: 10; user-select: none;">&#10095;</button>
          <div id="slider-badge" style="position: absolute; bottom: 15px; right: 15px; background: rgba(0,0,0,0.7); color: #fff; padding: 4px 10px; border-radius: 12px; font-size: 12px; font-family: sans-serif; font-weight: bold; z-index: 10;">1 / 2</div>
        </div>
      `;
      
      // Terapkan penimpaan struktur HTML
      themeContainer.innerHTML = sliderHTML;
    }

    // Ambil referensi objek DOM slider yang baru saja di-render
    const sliderWrapper = document.querySelector('.cert-slider-slides');
    const slideImages = document.querySelectorAll('.cert-slide-img');
    const sliderPrev = document.getElementById('slider-prev');
    const sliderNext = document.getElementById('slider-next');
    const sliderBadge = document.getElementById('slider-badge');

    // Referensi komponen DOM Lightbox (Mode Teater)
    const lightbox = document.getElementById('custom-light-box');
    const lightboxImg = document.getElementById('light-box-img');
    const closeBtn = document.getElementById('close-light-box');
    const prevBtn = document.getElementById('prev-light-box');
    const nextBtn = document.getElementById('next-light-box');

    // Fungsi Pergeseran Konten Slider Utama
    function updateSlider(index) {
      currentIdx = index;
      if (sliderWrapper) sliderWrapper.style.transform = `translateX(-${currentIdx * 100}%)`;
      if (sliderBadge) sliderBadge.textContent = `${currentIdx + 1} / ${imagesList.length}`;
    }

    if (sliderPrev && sliderNext) {
      sliderPrev.addEventListener('click', function(e) {
        e.stopPropagation(); // Mencegah pemicu event klik gambar pembuka teater
        let newIdx = currentIdx - 1;
        if (newIdx < 0) newIdx = imagesList.length - 1;
        updateSlider(newIdx);
      });

      sliderNext.addEventListener('click', function(e) {
        e.stopPropagation();
        let newIdx = currentIdx + 1;
        if (newIdx >= imagesList.length) newIdx = 0;
        updateSlider(newIdx);
      });
    }

    // Fungsi Pengendali Tampilan Gambar di Layar Teater (Lightbox)
    function updateLightboxImage(index) {
      currentIdx = index;
      lightboxImg.src = imagesList[currentIdx];
    }

    function openLightbox(index) {
      updateLightboxImage(index);
      lightbox.style.display = 'flex';
      setTimeout(() => { lightbox.style.opacity = '1'; }, 10);
      document.body.style.overflow = 'hidden';
    }

    const closeLightbox = function() {
      lightbox.style.opacity = '0';
      setTimeout(() => { 
        lightbox.style.display = 'none'; 
        document.body.style.overflow = ''; 
        updateSlider(currentIdx); // Sinkronisasi posisi slider luar saat teater ditutup
      }, 200);
    };

    // Daftarkan aksi klik pembukaan teater pada setiap slide gambar
    slideImages.forEach((img, index) => {
      img.addEventListener('click', function() { openLightbox(index); });
    });

    // Kontrol tombol navigasi internal di dalam Teater Lightbox
    prevBtn.addEventListener('click', function(e) {
      e.stopPropagation();
      let newIdx = currentIdx - 1;
      if (newIdx < 0) newIdx = imagesList.length - 1;
      updateLightboxImage(newIdx);
    });

    nextBtn.addEventListener('click', function(e) {
      e.stopPropagation();
      let newIdx = currentIdx + 1;
      if (newIdx >= imagesList.length) newIdx = 0;
      updateLightboxImage(newIdx);
    });

    closeBtn.addEventListener('click', closeLightbox);
    lightbox.addEventListener('click', function(e) {
      if (e.target === lightbox || e.target === lightboxImg) { closeLightbox(); }
    });

    // Integrasi Pintasan Akses Keyboard (Panah Kiri, Kanan, & ESC)
    document.addEventListener('keydown', function(e) {
      if (lightbox.style.display === 'flex') {
        if (e.key === "Escape") closeLightbox();
        if (e.key === "ArrowLeft") prevBtn.click();
        if (e.key === "ArrowRight") nextBtn.click();
      } else {
        if (e.key === "ArrowLeft" && sliderPrev) sliderPrev.click();
        if (e.key === "ArrowRight" && sliderNext) sliderNext.click();
      }
    });
  });
</script>