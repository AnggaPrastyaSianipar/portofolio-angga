---
title: "Sertifikasi Kompetensi: Belajar Fundamental Front-End Web Development"
date: 2024-11-02

event: "Dicoding Indonesia - Google Developers Authorized Training Partner"
event_url: "https://dicoding.com"

location: "Dicoding Academy Indonesia"
address:
  country: "Indonesia"

summary: "Sertifikat Kompetensi Resmi dari Dicoding Indonesia dengan kurikulum standar Google Developers untuk penguasaan arsitektur ES6+, Web Components yang reusable, Package Manager, Module Bundler (Webpack), dan AJAX/Fetch API."

# Waktu pelaksanaan mengikuti tanggal terbit sertifikat Anda (02 November 2024)
event_start: 2024-11-02
event_end: 2024-11-02
all_day: true

authors:
  - me

tags:
  - Certification
  - Front-End Development
  - Web Components
  - Webpack
  - ES6
  - Google Developers

featured: true

image:
  caption: "Sertifikat Dicoding Fundamental Front-End Web Development (Klik gambar untuk Mode Theater HD)"
  focal_point: "Center"
---

## 📜 Deskripsi Sertifikasi Kompetensi

Sertifikat dengan nomor **JLX149MJ2X72** ini diterbitkan oleh **Dicoding Indonesia** selaku **Google Developers Authorized Training Partner**. Kredensial tingkat menengah (*Intermediate*) ini memvalidasi kapabilitas standar industri dalam membangun aplikasi front-end web yang dinamis, terstruktur, optimal, serta menerapkan komponen antarmuka yang bersifat *reusable* dan *encapsulated*.

---

### 🏛️ Kompetensi Inti yang Divalidasi
Kurikulum kurasi mendalam sepanjang **80 Jam** ini mencakup penguasaan aspek arsitektur front-end modern:
* **Sintaksis JavaScript Modern (ES6+)**: Menguasai dan menerapkan fitur modern seperti *block scope variable, template literals, destructuring object & array, spread operator, rest parameters, arrow function, JavaScript Classes, Promise*, hingga *JavaScript Modules*.
* **Teknik Layouting Lanjut (CSS Grid)**: Mahir menyusun tata letak tata ruang dan grid antarmuka web yang kompleks, responsif, dan fleksibel menggunakan metode *CSS Grid System*.
* **Arsitektur Web Component**: Membangun komponen UI (*User Interface*) kustom yang bersifat *reusable* (dapat digunakan kembali) dan *encapsulated* (terisolasi) memanfaatkan teknologi *custom elements* dan *Shadow DOM*.
* **Manajemen Package & Build Tools (Webpack)**: Mengelola dependensi proyek (*development & production dependencies*) via Node Package Manager (NPM) serta mahir menerapkan *Webpack* sebagai *module bundler* untuk menyatukan dan mengoptimalkan aset kode menjadi berkas statis siap rilis.
* **Asynchronous Request & REST API**: Melakukan transaksi data dinamis secara asinkronus dengan *Web Server* melalui protokol HTTP/HTTPS menggunakan *Fetch API* (mengimplementasikan metode *GET, POST, PUT,* dan *DELETE*).

---

### 🛠️ Proyek Akhir Kelulusan (Submissions)
Untuk berhak mendapatkan kelulusan, saya telah menyelesaikan dua proyek implementation nyata:
1. **Proyek Pertama**: Membangun aplikasi web interaktif dengan memanfaatkan fitur *HTML Form* lanjutan, teknik tata letak *CSS Grid*, dan enkapsulasi *Web Component*.
2. **Proyek Kedua (Utama)**: Mengembangkan aplikasi web dinamis yang mengintegrasikan manajemen package NPM, pembundelan aset otomatis via *Webpack*, serta penyajian data real-time yang dikonsumsi langsung dari *REST API* menggunakan operasi asynchronous JavaScript.

> [!SUCCESS] Masa Berlaku Kredensial
> Sertifikat ini ditetapkan pada **02 November 2024** dan berlaku selama **3 (tiga) tahun** hingga 02 November 2027 di bawah otoritas Narenda Wicaksono selaku Chief Executive Officer Dicoding Indonesia.


<div id="custom-light-box" style="display: none; position: fixed; top: 0; left: 0; width: 100vw; height: 100vh; background: rgba(5, 5, 5, 0.98); z-index: 999999; justify-content: center; align-items: flex-start; padding: 50px 20px; opacity: 0; transition: opacity 0.2s ease-in-out; overflow-y: auto;">
  <span id="close-light-box" style="position: fixed; top: 20px; right: 30px; color: #fff; font-size: 50px; font-weight: bold; cursor: pointer; user-select: none; z-index: 1000001; background: rgba(0,0,0,0.6); width: 60px; height: 60px; display: flex; justify-content: center; align-items: center; border-radius: 50%; line-height: 50px;">&times;</span>
  
  <span id="prev-light-box" style="position: fixed; top: 50%; left: 30px; color: #fff; font-size: 40px; font-weight: bold; cursor: pointer; user-select: none; z-index: 1000001; background: rgba(0,0,0,0.6); width: 50px; height: 50px; display: flex; justify-content: center; align-items: center; border-radius: 50%; transform: translateY(-50%);">&#10094;</span>
  <span id="next-light-box" style="position: fixed; top: 50%; right: 30px; color: #fff; font-size: 40px; font-weight: bold; cursor: pointer; user-select: none; z-index: 1000001; background: rgba(0,0,0,0.6); width: 50px; height: 50px; display: flex; justify-content: center; align-items: center; border-radius: 50%; transform: translateY(-50%);">&#10095;</span>

  <img id="light-box-img" src="" alt="Sertifikat Ultra HD" style="display: block; width: 100%; max-width: 1400px; height: auto; border-radius: 6px; box-shadow: 0 0 40px rgba(255,255,255,0.3); margin: 0 auto; cursor: zoom-out; image-rendering: -webkit-optimize-contrast; image-rendering: crisp-edges; transform: translateZ(0); backface-visibility: hidden;">
</div>

<script>
  document.addEventListener("DOMContentLoaded", function() {
    // 1. Array berisi 3 nama file gambar sertifikat Anda
    const imagesList = ["featured.jpg", "featured1.jpg", "featured2.jpg"];
    let currentIdx = 0;

    // 2. Cari target elemen gambar bawaan dari template website Anda
    const themeImgTarget = document.querySelector('.article-header img, .featured-image img, img[class*="featured"]');
    
    if (themeImgTarget) {
      const themeContainer = themeImgTarget.parentElement;
      
      // Mengubah kontainer gambar tunggal statis bawaan tema menjadi struktur Slider 3 Gambar
      const sliderHTML = `
        <div class="cert-slider-container" style="position: relative; width: 100%; max-width: 800px; margin: 0 auto 20px auto; overflow: hidden; border-radius: 8px; box-shadow: 0 4px 20px rgba(0,0,0,0.3);">
          <div class="cert-slider-slides" style="display: flex; transition: transform 0.4s ease-in-out;">
            <img class="cert-slide-img" src="featured.jpg" style="width: 100%; flex-shrink: 0; cursor: zoom-in;" title="Klik untuk Mode Theater HD">
            <img class="cert-slide-img" src="featured1.jpg" style="width: 100%; flex-shrink: 0; cursor: zoom-in;" title="Klik untuk Mode Theater HD">
            <img class="cert-slide-img" src="featured2.jpg" style="width: 100%; flex-shrink: 0; cursor: zoom-in;" title="Klik untuk Mode Theater HD">
          </div>
          <button id="slider-prev" style="position: absolute; top: 50%; left: 15px; transform: translateY(-50%); background: rgba(0,0,0,0.6); color: white; border: none; width: 45px; height: 45px; border-radius: 50%; font-size: 24px; cursor: pointer; display: flex; align-items: center; justify-content: center; z-index: 10; user-select: none;">&#10094;</button>
          <button id="slider-next" style="position: absolute; top: 50%; right: 15px; transform: translateY(-50%); background: rgba(0,0,0,0.6); color: white; border: none; width: 45px; height: 45px; border-radius: 50%; font-size: 24px; cursor: pointer; display: flex; align-items: center; justify-content: center; z-index: 10; user-select: none;">&#10095;</button>
          <div id="slider-badge" style="position: absolute; bottom: 15px; right: 15px; background: rgba(0,0,0,0.7); color: #fff; padding: 4px 10px; border-radius: 12px; font-size: 12px; font-family: sans-serif; font-weight: bold; z-index: 10;">1 / 3</div>
        </div>
      `;
      
      // Lakukan injeksi struktur HTML slider baru
      themeContainer.innerHTML = sliderHTML;
    }

    // Ambil ulang elemen DOM slider baru hasil injeksi
    const sliderWrapper = document.querySelector('.cert-slider-slides');
    const slideImages = document.querySelectorAll('.cert-slide-img');
    const sliderPrev = document.getElementById('slider-prev');
    const sliderNext = document.getElementById('slider-next');
    const sliderBadge = document.getElementById('slider-badge');

    // Referensi komponen DOM Lightbox (Mode Teater)
    const lightbox = document.getElementById('custom-light-box');
    const lightboxImg = document.getElementById('light-box-img');
    const closeBtn = document.getElementById('close-light-box');
    const prevBtn = document.getElementById('prev-light-box');
    const nextBtn = document.getElementById('next-light-box');

    // Logika Pergeseran Slider Utama di Halaman Artikel
    function updateSlider(index) {
      currentIdx = index;
      if (sliderWrapper) sliderWrapper.style.transform = `translateX(-${currentIdx * 100}%)`;
      if (sliderBadge) sliderBadge.textContent = `${currentIdx + 1} / ${imagesList.length}`;
    }

    if (sliderPrev && sliderNext) {
      sliderPrev.addEventListener('click', function(e) {
        e.stopPropagation(); // Mencegah pemicu event klik gambar pembuka lightbox
        let newIdx = currentIdx - 1;
        if (newIdx < 0) newIdx = imagesList.length - 1;
        updateSlider(newIdx);
      });

      sliderNext.addEventListener('click', function(e) {
        e.stopPropagation();
        let newIdx = currentIdx + 1;
        if (newIdx >= imagesList.length) newIdx = 0;
        updateSlider(newIdx);
      });
    }

    // Logika Pengendali Gambar di Layar Teater (Lightbox)
    function updateLightboxImage(index) {
      currentIdx = index;
      lightboxImg.src = imagesList[currentIdx];
    }

    function openLightbox(index) {
      updateLightboxImage(index);
      lightbox.style.display = 'flex';
      setTimeout(() => { lightbox.style.opacity = '1'; }, 10);
      document.body.style.overflow = 'hidden';
    }

    const closeLightbox = function() {
      lightbox.style.opacity = '0';
      setTimeout(() => { 
        lightbox.style.display = 'none'; 
        document.body.style.overflow = ''; 
        updateSlider(currentIdx); // Sinkronisasi halaman luar saat teater ditutup
      }, 200);
    };

    // Daftarkan aksi klik perbesaran teater ke setiap dari 3 gambar slide
    slideImages.forEach((img, index) => {
      img.addEventListener('click', function() { openLightbox(index); });
    });

    // Kontrol navigasi panah internal di dalam Teater Lightbox
    prevBtn.addEventListener('click', function(e) {
      e.stopPropagation();
      let newIdx = currentIdx - 1;
      if (newIdx < 0) newIdx = imagesList.length - 1;
      updateLightboxImage(newIdx);
    });

    nextBtn.addEventListener('click', function(e) {
      e.stopPropagation();
      let newIdx = currentIdx + 1;
      if (newIdx >= imagesList.length) newIdx = 0;
      updateLightboxImage(newIdx);
    });

    closeBtn.addEventListener('click', closeLightbox);
    lightbox.addEventListener('click', function(e) {
      if (e.target === lightbox || e.target === lightboxImg) { closeLightbox(); }
    });

    // Integrasi Pintasan Akses Keyboard (Panah Kiri, Kanan, & ESC)
    document.addEventListener('keydown', function(e) {
      if (lightbox.style.display === 'flex') {
        if (e.key === "Escape") closeLightbox();
        if (e.key === "ArrowLeft") prevBtn.click();
        if (e.key === "ArrowRight") nextBtn.click();
      } else {
        if (e.key === "ArrowLeft" && sliderPrev) sliderPrev.click();
        if (e.key === "ArrowRight" && sliderNext) sliderNext.click();
      }
    });
  });
</script>